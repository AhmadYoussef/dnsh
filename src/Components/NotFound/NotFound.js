import React from 'react';
import classes from './NotFound.module.scss';

const NotFound = () =>{
    return(
        <main className={classes.notFound}>
           <h1>404 This Page NotFound</h1>
        </main>
    );
}

export default NotFound;